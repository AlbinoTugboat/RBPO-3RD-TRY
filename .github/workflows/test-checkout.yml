name: Debug Full CI Path

on:
  pull_request:
    branches: [ main ]

jobs:
  build-simulate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code (Trying main CI config)
        uses: actions/checkout@v4
        # Оставляем ТОЧНО ТАК ЖЕ, как у вас сейчас в ci.yml

      - name: Simulate Maven Cache
        run: echo "Simulating cache step..."

      - name: Simulate Maven Build
        run: echo "Simulating build..."

      - name: Upload source code for SAST
        uses: actions/upload-artifact@v4
        with:
          name: source-code
          path: ./

  semgrep-simulate:
    needs: build-simulate
    runs-on: ubuntu-latest
    steps:
      - name: Download source code
        uses: actions/download-artifact@v4
        with:
          name: source-code

      - name: Run Fake Semgrep
        run: echo "SAST scan would happen here"